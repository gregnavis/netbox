#ifndef MAX_PACKET_SIZE
#define MAX_PACKET_SIZE 2048
#endif

#define MAX_HEXDUMP_SIZE (2 * MAX_PACKET_SIZE)
#define MAX_LINE_SIZE (MAX_HEXDUMP_SIZE - 1)

#define MAX_PROTOCOL_SIZE 15

#define MAX_PATH_SIZE 255
